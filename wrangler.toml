name = "market-intel"
pages_build_output_dir = "dist"

# ── D1 Database ───────────────────────────────────────────────────────────
# Setup:
#   1. wrangler d1 create market-intel
#   2. Copia el database_id al campo id de abajo
#   3. wrangler d1 execute market-intel --file=schema.sql

[[d1_databases]]
binding     = "DB"
database_name = "market-intel"
database_id = ""          # ← rellenar tras: wrangler d1 create market-intel

# ── Variables de entorno (secrets) ───────────────────────────────────────
# wrangler pages secret put TELEGRAM_TOKEN
# wrangler pages secret put TELEGRAM_CHAT_ID
